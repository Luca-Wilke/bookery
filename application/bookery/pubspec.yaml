name: bookery
description: Bookery is a social app allowing users to read, write and rate book summaries.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3

  ###################
  #IMPORTANT PACKAGES
  ###################

  #google fonts to dynamically load and use free available fonts by the google fonts api
  #android, ios
  #google_fonts: ^1.1.0

  #auto size text to automatically increase / decrease the font size to fit the maximum number of lines and avoid out of space exceptions
  #android, ios, web
  #auto_size_text: ^2.1.0

  #getwidget is a ui / widget library
  #android, ios, web
  #getwidget: ^1.1.0

  #redux libraries to use a redux software architecture in flutter
  #android, ios, web
  redux: ^4.0.0
  flutter_redux: ^0.6.0
  #redux_thunk: ^0.3.0
  #redux_api_middleware: ^0.2.1

  #firebase libraries
  #android, ios, web
  #firebase_auth: ^0.16.1
  #google_sign_in: ^4.5.1
  #todo: install other required firebase packages like firestore

  ###############
  #EXTRA PACKAGES
  ###############

  #todo: install and choose extra packages

  ##################
  #OPTIONAL PACKAGES
  ##################

  #todo: install and choose optional packages


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true